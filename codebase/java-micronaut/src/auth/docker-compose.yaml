version: '3.9'
services:
  db:
    image: postgres:latest
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: auth_user
      POSTGRES_PASSWORD: pwd1235
      POSTGRES_DB: auth
    ports:
      - "5433:5432"

